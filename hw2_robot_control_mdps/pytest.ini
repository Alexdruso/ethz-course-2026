[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    baseline: pins the reward function described in the handout; deselect with -m "not baseline" once you redesign it for the ex3 bonus
